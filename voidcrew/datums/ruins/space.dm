// Voidcrew space ruin templates.
//
// NOTE: this file once held 11 templates (Fast_Food, scav_mining,
// power_puzzle, spacegym, oldcodeops, transport18, fueldepot, nuclear_dump,
// ntfacility, astraeus, dark_glade) whose .dmm files never made it into this
// repo — dead entries poisoned the space-ruin picker (signals that rolled
// them could never load on dock), so they were removed 2026-07-16. The space
// pool still draws from upstream /tg/ space ruins, the rare raid ruins
// (rare_space.dm) and the vestige ruins (antag_space.dm).
//
// If you add a template here, make sure its map actually exists under
// _maps/voidcrew/RandomRuins/SpaceRuins/.
