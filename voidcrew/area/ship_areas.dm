/area/centcom/asteroid/voidcrew
	area_flags = UNIQUE_AREA | CAVES_ALLOWED | MOB_SPAWN_ALLOWED
	// Already fullbright via inherited luminosity = 1; nothing here is dark or uses placed
	// lights, so the dynamic-lighting engine (corners, light tracking) is pure overhead.
	static_lighting = FALSE
