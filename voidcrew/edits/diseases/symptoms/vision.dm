/**
 * # Voidcrew virology: symptom tiers (eyesight)
 *
 * Level re-tier from tgstation #84356 / #89062 (hyperjll). Hyphema moves 5 -> 6. It is the
 * cheapest way to blind a hostile crew member for several minutes, so it climbs just above the
 * nuisance tier rather than sharing it with sneezing.
 *
 * Level only: stats, thresholds and procs are all inherited. See
 * code/modules/unit_tests/voidcrew_virology_tiers.dm for the whole map.
 */
/datum/symptom/visionloss
	level = 6
